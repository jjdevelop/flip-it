'use strict';

describe('Phaser Game Functions', function() {

     // beforeEach(module('flipit'));

    describe('Toggle Squares', function() {
    	var modifyingSquares;

        beforeEach(inject(function () {
        	// Create Squares
            modifyingSquares = [];
        }));

        it('should have the expected type properties', function() {
            expect(modifyingSquares).toBeDefined();
        });

    });

});
